// users 数据层
const dao = require('../config/dao')

const indexDAL = {
    getTour: (user_id,cb) => {
        
    },

    getBlog: (user_id,cb) => {
        
    },

    getPack: (user_id,cb) => {
        
    },
}

module.exports = indexDAL